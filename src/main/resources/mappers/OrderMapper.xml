<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.homeward.webstore.mapper.OrderMapper">
    <cache/>
    <select id="getTotalPrice" resultType="Float" useCache="true">
        SELECT
        SUM((i.price / 100) * (i.onsale_percent / 100) * o.amount) price
        FROM minecraft_webstore.order o
        LEFT JOIN items i
        ON o.item_id = i.id
        <where>
            o.uuid = #{uuid}
        </where>
    </select>

    <insert id="insertCart" flushCache="true">
        INSERT INTO
        minecraft_webstore.order(UUID, item_id, amount)
        VALUES(#{uuid}, #{itemId}, #{amount})
    </insert>

    <update id="updateCart" flushCache="true">
        UPDATE minecraft_webstore.order
        <set>
            <if test="amount != null and amount != ''">
                amount = #{amount}
            </if>
        </set>
        <where>
            uuid = #{uuid}
            AND item_id = #{itemId}
        </where>
    </update>

    <delete id="deleteCart" flushCache="true">
        DELETE FROM minecraft_webstore.order
        <where>
            UUID = #{uuid}
            AND item_id = #{itemId}
        </where>
    </delete>

    <delete id="initializeCart" flushCache="true">
        DELETE FROM minecraft_webstore.order
    </delete>
</mapper>